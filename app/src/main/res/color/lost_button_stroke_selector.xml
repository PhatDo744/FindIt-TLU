<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Viền sẽ có màu của nút khi không được chọn -->
    <item android:color="@color/lost_red" android:state_selected="false"/>
    <!-- Viền sẽ trong suốt (ẩn đi) khi được chọn -->
    <item android:color="@android:color/transparent" android:state_selected="true"/>
    <!-- Trạng thái mặc định -->
    <item android:color="@color/lost_red"/>
</selector>